refactor: implement responsive cart grid layout and modular notification system

CART PAGE REFINEMENTS
- Refactor `Cart.jsx` to use a complex responsive grid system (`sm:grid-cols-7`, `sm:grid-rows-[1fr_auto]`) for better information density
- Reposition product info and pricing: Info takes 3 columns, Price takes 2 columns in Row 1, and the Delete Button is placed in Row 2
- Align Delete Button to bottom-right (`items-end justify-end`) of its grid area on large screens
- Implement responsive padding and font sizing adjustments for `lg` and `sm` breakpoints

NOTIFICATION SYSTEM UPGRADES
- Transition from a standalone notification page to a modal-based architecture
- Create `NotificationModal.jsx` for displaying user alerts and system messages without navigating away from the current page
- Integrate notification trigger state into `TopNavbar` and `BottomNavBar` components
- Remove legacy `Notifications.jsx` file to streamline the codebase

LAYOUT & NAVIGATION FIXES
- Constrain `TopNavbar` width to match page content for better horizontal alignment
- Center `PersonalInfo` and `Profile` page content horizontally for consistent visual balance
- Adjust mobile navigation layering (z-index) to prevent overlap issues with sidebars
- Increase font sizes and icon dimensions for `SellerProfile` links on medium to large screens

COMPONENT ENHANCEMENTS
- Refine product detail layout in `ProductPage` for better image and thumbnail positioning
- Clean up icon weights across the project and ensure consistent usage of `react-icons/pi`
