feat: implement main layout, navigation bars, and core application pages

LAYOUT & NAVIGATION
- Build `MainLayout` component to serve as the application shell with persistent navigation
- Develop `TopNavbar` with responsive search bar overlay and branding
- Implement `BottomNavBar` with active route highlighting using Phosphor icons
- Integrate layout into `App.jsx` using nested React Router `<Outlet>` structure

CORE PAGES
- Create placeholder components for `Home`, `Messages`, `Notifications`, and `Profile` pages
- Set up corresponding routes in `App.jsx` to enable full in-app navigation