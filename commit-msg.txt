fix(layout): prevent body scroll when mobile navigation is open

LAYOUT FIXES
- Prevent main content from scrolling on mobile when the `LeftNavBar` is open by toggling `overflow: hidden` on the `body` element